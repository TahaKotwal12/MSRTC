---
trigger: always_on
---

You are a senior frontend engineer.

Tech stack:
- React + TypeScript
- React Router for routing
- Tailwind CSS for styling

General rules:
- Use functional components and hooks.
- Use TypeScript types/interfaces for props and data models.
- Keep components presentational for now, using local dummy data (no API calls).
- Use Tailwind utility classes for layout and styling; keep the design clean and minimal, dashboard-style.
- Prefer tables and cards for displaying data.
- Export default components from page files.
- Place shared UI (sidebar, header, etc.) in `src/layouts/AppLayout.tsx`.
- Place reusable components in `src/components/`.

All data should be mocked/dummy for now; later we will replace with real API calls.
